import 'package:flutter/widgets.dart';

import '../rekomendasi_pakan/rekomendasi_pakan_screen.dart';

// Wrapper sementara untuk menjaga kompatibilitas import lama.
class FormulasiRansumScreen extends StatelessWidget {
  const FormulasiRansumScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const RekomendasiPakanScreen();
  }
}
